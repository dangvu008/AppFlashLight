<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dang.agi.appflashlight">

    <uses-permission android:name="android.permission.CAMERA" />

    <permission
        android:name="android.permission.FLASHLIGHT"
        android:permissionGroup="android.permission-group.HARDWARE_CONTROLS"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".FlashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FlashLight" />
        <activity android:name=".FlashBulb" />
        <activity android:name=".TrafficActivity" />
        <activity android:name=".FlashLightActivity" />
        <activity android:name=".WarringActivity" />
        <activity
            android:name=".PoliceActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".Warring2" />
        <activity android:name=".ScreenLight"></activity>
    </application>

</manifest>